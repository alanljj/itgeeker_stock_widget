# 2026-04-10 工作日志

## v1.1.3 变更记录（main.py）

### 需求 12 - 分组列表多选 checkbox
- 每行分组前新增 `QCheckBox`，勾选后可批量操作
- 按钮区增加「全选」按钮（动态切换文字为「取消全选」）和「打开已选」按钮
- `_row_checkboxes` 列表跟踪所有行的 checkbox 引用
- 新增方法：`_sync_select_all_btn()`, `toggle_select_all()`, `launch_selected()`

### 需求 13 - 系统托盘图标变大
- `create_emoji_icon("💹", 64)` 从 32px 改为 64px，提升托盘可视性

### 需求 14 - 程序标题统一为 ITGeeker Stock Widget
- `APP_NAME` 改为 `"ITGeeker Stock Widget"`
- 窗口标题: `f"ITGeeker Stock Widget {APP_VERSION}"`
- 主面板 header、closeEvent 托盘通知均已同步更新

### 需求 15 - 托盘 tooltip 分行显示
- tooltip 格式改为三行：`APP_NAME\n{APP_VERSION}\n股票分组: X 个`（之前标题和版本在同一行）

### 版本号
- `APP_VERSION` 升至 `v1.1.3`

## 配置文件路径统一（本次会话）

### 变更内容
- 配置文件路径从工作目录 `categories.json` 改为用户目录 `~/itgeeker_widget_config/config_stock.json`
- `load_config()`：优先读新路径，若无则自动迁移旧 `categories.json`，再写回新路径
- `save_config()`：自动创建目录后写入（`os.makedirs(exist_ok=True)`）
- 兼容迁移逻辑：首次启动时自动把旧配置文件迁移到新位置

### 新路径
```
C:\Users\<用户名>\itgeeker_widget_config\config_stock.json
```

## 打包记录
- 同步更新 `file_version_info.txt` 版本号为 1.1.3，ProductName 改为 `ITGeeker Stock Widget`
- 命令: `pyinstaller ITGeekerStockWidget.spec --clean`
- 输出: `dist/ITGeekerStockWidget.exe`，大小 **51.90 MB**，生成时间 2026/4/10 11:18

### v1.3.0.0 打包（13:10）
- `APP_VERSION = "v1.3.0.0"`
- `file_version_info.txt`：`filevers/prodvers = (1, 3, 0, 0)`，`FileVersion/ProductVersion = 1.3.0.0`
- 输出: `dist/ITGeekerStockWidget.exe`，51.90 MB
